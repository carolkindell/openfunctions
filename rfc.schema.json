{
  "servers": {
    "type": "array",
    "description": "Server information for API endpoints",
    "items": {
      "type": "object",
      "required": ["url"],
      "properties": {
        "url": {
          "type": "string",
          "description": "URL of the server"
        },
        "protocol": {
          "type": "string",
          "description": "Protocol used by the server"
        }
      }
    }
  },
  "state": {
    "description": "State definition for the module/package",
    "$ref": "#/definitions/schema"
  }
}
